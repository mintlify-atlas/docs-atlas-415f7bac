---
title: Garage Documentation
description: S3-compatible object storage for small self-hosted geo-distributed deployments
mode: custom
---

<div className="relative overflow-hidden bg-gradient-to-br from-[#0f1117] via-[#1a1d27] to-[#0f1117] dark:from-[#0f1117] dark:via-[#1a1d27] dark:to-[#0f1117] py-20">
  <div className="max-w-7xl mx-auto px-6 lg:px-8">
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
      <div className="lg:col-span-7">
        <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold text-white mb-6">
          S3-Compatible Object Storage for Self-Hosted Deployments
        </h1>
        <p className="text-lg sm:text-xl text-gray-300 dark:text-gray-300 max-w-2xl mb-8">
          Garage is a lightweight, geo-distributed object storage service designed for small to medium-scale self-hosting. Store your data across multiple locations with automatic replication and S3 compatibility.
        </p>
        <div className="flex flex-wrap gap-4">
          <a
            href="/quick-start"
            className="inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-white bg-[#fa932c] hover:bg-[#e8822a] rounded-lg transition-colors"
          >
            Get Started
          </a>
          <a
            href="/api/overview"
            className="inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-white border border-white/30 bg-white/10 hover:bg-white/20 rounded-lg transition-colors"
          >
            API Reference
          </a>
        </div>
      </div>
      <div className="lg:col-span-5">
        <div className="relative">
          <div className="absolute inset-0 bg-gradient-to-r from-[#fa932c] to-[#a8713b] rounded-2xl blur-3xl opacity-20"></div>
          <div className="relative bg-[#1a1d27] dark:bg-[#1a1d27] border border-[#27272a] dark:border-[#27272a] rounded-2xl p-6">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-3 h-3 rounded-full bg-red-500"></div>
              <div className="w-3 h-3 rounded-full bg-yellow-500"></div>
              <div className="w-3 h-3 rounded-full bg-green-500"></div>
            </div>
            <pre className="text-sm text-gray-300 dark:text-gray-300 overflow-x-auto">
              <code>{`# Start a single-node cluster
garage server --single-node \\
  --default-bucket

# Upload files with S3 API
aws s3 cp myfile.txt \\
  s3://my-bucket/

# Automatic replication
# across geo-distributed nodes`}</code>
            </pre>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
    Quick Start
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Get Garage running in minutes with these simple steps
  </p>

  <Steps>
    <Step title="Download Garage">
      Download the latest binary for your platform from the [official releases](https://garagehq.deuxfleurs.fr/download/) or install via your package manager.

      ```bash
      # Place binary in your PATH
      sudo cp garage /usr/local/bin/
      sudo chmod +x /usr/local/bin/garage
      ```
    </Step>

    <Step title="Create Configuration">
      Generate a minimal configuration file with unique secrets:

      ```bash
      cat > garage.toml <<EOF
      metadata_dir = "/var/lib/garage/meta"
      data_dir = "/var/lib/garage/data"
      db_engine = "sqlite"
      
      replication_factor = 1
      
      rpc_bind_addr = "[::]:3901"
      rpc_public_addr = "127.0.0.1:3901"
      rpc_secret = "$(openssl rand -hex 32)"
      
      [s3_api]
      s3_region = "garage"
      api_bind_addr = "[::]:3900"
      
      [admin]
      api_bind_addr = "[::]:3903"
      admin_token = "$(openssl rand -base64 32)"
      EOF
      ```
    </Step>

    <Step title="Start the Server">
      Launch Garage with automatic single-node configuration:

      ```bash
      export GARAGE_CONFIG_FILE=./garage.toml
      export GARAGE_DEFAULT_ACCESS_KEY="GK$(openssl rand -hex 16)"
      export GARAGE_DEFAULT_SECRET_KEY="$(openssl rand -hex 32)"
      export GARAGE_DEFAULT_BUCKET="default-bucket"
      
      garage server --single-node --default-bucket
      ```

      <Note>
        The `--single-node` flag automatically configures a single-node cluster. The `--default-bucket` flag creates a default access key and bucket using the environment variables.
      </Note>
    </Step>

    <Step title="Upload Your First Object">
      Configure the AWS CLI and upload a file:

      ```bash
      # Configure AWS CLI
      export AWS_ENDPOINT_URL='http://localhost:3900'
      export AWS_DEFAULT_REGION='garage'
      export AWS_ACCESS_KEY_ID="$GARAGE_DEFAULT_ACCESS_KEY"
      export AWS_SECRET_ACCESS_KEY="$GARAGE_DEFAULT_SECRET_KEY"
      
      # Upload a file
      aws s3 cp myfile.txt s3://default-bucket/
      
      # List objects
      aws s3 ls s3://default-bucket/
      ```
    </Step>
  </Steps>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
    Why Garage?
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Built for small-scale self-hosting with enterprise-grade features
  </p>

  <CardGroup cols={2}>
    <Card title="Geo-Distributed by Design" icon="globe" href="/design/goals">
      Replicate data across multiple physical locations for maximum resilience. Garage handles network partitions and keeps your data available even when nodes go offline.
    </Card>
    <Card title="S3 Compatible" icon="cube" href="/api/s3-compatibility">
      Drop-in replacement for Amazon S3. Use existing S3 tools, libraries, and workflows without modification.
    </Card>
    <Card title="No External Dependencies" icon="box" href="/design/features">
      Self-contained binary with no database, no RAFT consensus, no ZooKeeper. Just start the server and you're running.
    </Card>
    <Card title="Lightweight & Fast" icon="bolt" href="/design/internals">
      Designed for consumer hardware and standard Internet connections. No RAFT bottleneck means better performance across distributed nodes.
    </Card>
    <Card title="Flexible Topology" icon="diagram-project" href="/operations/cluster-layout">
      Add or remove nodes at any time. Garage automatically rebalances data to maintain your desired replication level.
    </Card>
    <Card title="Built-in Web Server" icon="browser" href="/integration/websites">
      Host static websites directly from S3 buckets with custom domain support. No reverse proxy required.
    </Card>
  </CardGroup>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <h2 className="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
    Explore Documentation
  </h2>
  <p className="text-base text-gray-600 dark:text-gray-400 mb-8">
    Everything you need to deploy and operate Garage
  </p>

  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
    <a
      href="/installation/binary-packages"
      className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#fa932c] dark:hover:border-[#fa932c] overflow-hidden transition-colors no-underline bg-white dark:bg-[#1a1d27]"
    >
      <div className="p-6">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">
          Installation
        </h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Install Garage from binary packages, build from source, or run with Docker
        </p>
        <div className="flex items-center text-sm font-medium text-gray-900 dark:text-gray-300 group-hover:text-[#fa932c] dark:group-hover:text-[#fa932c]">
          View guides
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>

    <a
      href="/deployment/multi-node"
      className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#fa932c] dark:hover:border-[#fa932c] overflow-hidden transition-colors no-underline bg-white dark:bg-[#1a1d27]"
    >
      <div className="p-6">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">
          Deployment
        </h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Set up single-node or multi-node clusters with geo-distributed replication
        </p>
        <div className="flex items-center text-sm font-medium text-gray-900 dark:text-gray-300 group-hover:text-[#fa932c] dark:group-hover:text-[#fa932c]">
          View guides
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>

    <a
      href="/operations/cluster-layout"
      className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#fa932c] dark:hover:border-[#fa932c] overflow-hidden transition-colors no-underline bg-white dark:bg-[#1a1d27]"
    >
      <div className="p-6">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">
          Operations
        </h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Manage cluster layout, perform repairs, monitor health, and handle upgrades
        </p>
        <div className="flex items-center text-sm font-medium text-gray-900 dark:text-gray-300 group-hover:text-[#fa932c] dark:group-hover:text-[#fa932c]">
          View guides
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>

    <a
      href="/cli/overview"
      className="group block rounded-2xl border border-gray-200 dark:border-[#27272a] hover:border-[#fa932c] dark:hover:border-[#fa932c] overflow-hidden transition-colors no-underline bg-white dark:bg-[#1a1d27]"
    >
      <div className="p-6">
        <h3 className="text-base font-semibold text-gray-900 dark:text-white mb-2">
          CLI Reference
        </h3>
        <p className="text-sm text-gray-600 dark:text-gray-400 mb-3">
          Complete command-line reference for cluster administration and configuration
        </p>
        <div className="flex items-center text-sm font-medium text-gray-900 dark:text-gray-300 group-hover:text-[#fa932c] dark:group-hover:text-[#fa932c]">
          View reference
          <svg className="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
          </svg>
        </div>
      </div>
    </a>
  </div>
</div>

<div className="mt-16 mb-16 max-w-5xl mx-auto px-6">
  <div className="bg-gradient-to-r from-[#fa932c] to-[#a8713b] rounded-2xl p-8 md:p-12">
    <div className="max-w-3xl">
      <h2 className="text-2xl sm:text-3xl font-bold text-white mb-4">
        Ready to Deploy?
      </h2>
      <p className="text-lg text-white/90 mb-6">
        Start with a single node and scale to a geo-distributed cluster as your needs grow. Garage makes it easy.
      </p>
      <a
        href="/quick-start"
        className="inline-flex items-center justify-center px-6 py-3 text-base font-semibold text-[#fa932c] bg-white hover:bg-gray-100 rounded-lg transition-colors"
      >
        Get Started Now
      </a>
    </div>
  </div>
</div>
